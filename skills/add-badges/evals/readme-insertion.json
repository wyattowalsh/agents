{
  "skills": ["add-badges"],
  "query": "/add-badges --profile active --style flat-square --yes",
  "files": [],
  "expected_behavior": [
    "Agent parses flags: --profile active (8-12 badges: core + quality, code-style, frameworks), --style flat-square, --yes (skip approval)",
    "Agent runs detection (Phase 1) to identify repo features",
    "Agent selects 8-12 badges per the 'active' profile, grouped by display super-groups: Status > Quality > Package > Tech Stack > Social",
    "Agent ensures every badge uses dynamic endpoints (never hardcoded values) and includes logo parameters (?logo={slug}&logoColor=white)",
    "Agent presents grouped preview with category headers in Phase 3 (Present)",
    "Agent inserts badges between <!-- BADGES:START --> and <!-- BADGES:END --> markers in Phase 4 (Insert), creating markers if absent",
    "Agent adds <!-- generated by add-badges YYYY-MM-DD --> comment inside the marker block",
    "Agent skips the approval prompt because --yes was passed"
  ]
}
