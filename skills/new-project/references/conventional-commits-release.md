# Conventional Commits And Release Strategy

Use Conventional Commits as the default documented commit convention. Do not create commits from this skill unless the user explicitly requests git operations.

Use Changesets for publishable packages and monorepos. Use GitHub Releases only after a versioning strategy exists. Release publication is an external side effect and requires explicit approval.
