#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"${SCRIPT_DIR}/ensure-running.sh"
if command -v perplexity >/dev/null 2>&1; then
  exec "${MCPHUB_WRAPPED_PERPLEXITY:-perplexity}" "$@"
fi
exec open -a "Perplexity" --args "$@"
