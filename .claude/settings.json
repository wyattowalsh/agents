{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | xargs -I{} sh -c 'case \"{}\" in *.py) ruff check --fix --quiet \"{}\" 2>/dev/null ;; *.js|*.ts|*.jsx|*.tsx) pnpm exec prettier --write \"{}\" 2>/dev/null ;; esac' || true"
          }
        ]
      }
    ]
  }
}
