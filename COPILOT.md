@instructions/global.md

## GitHub Copilot CLI

- Follow repository conventions from `AGENTS.md`.
- Use `gpt-5.3-codex` for all work.
- Use `xhigh` reasoning effort for every task.
- Apply this model policy to direct execution, all subagents, teammate waves, and `/fleet` members.
- If another instruction specifies a different model (for example `opus`), this GitHub Copilot CLI policy takes precedence.
