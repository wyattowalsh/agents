name: dev-harness
host: 127.0.0.1
port: 8790
path: /mcp

public_url: https://mcp.w4w.dev/mcp

collection: collection.mcp.json
policy: policy.yaml
runtime_config: .runtime/mcp-proxy.config.json

auth:
  type: bearer
  env: CHATGPT_MCP_GATEWAY_TOKEN

tools:
  prefix_with_server: true
  separator: "__"
