# Plugin Ecosystem

## Objective

Document when native plugin packages should be used instead of direct skill/MCP/config projection.

## Rule

Use native plugins when the harness supports packaging multiple components together and the plugin gives better discovery, versioning, updates, or UX. Do not use plugins to fork canonical behavior.
