---
title: "MCP Servers"
description: "Model Context Protocol servers providing tools and data to AI agents"
---
{/* HAND-MAINTAINED */}

import { Card, CardGrid, Badge, Aside, Steps, LinkCard } from '@astrojs/starlight/components';

> Hand-maintained overview of MCP configuration. Prefer the registry SSOT and first-party package pages for accuracy.

<Badge text="67 configured servers" variant="note" size="large" />

<Aside type="tip" title="What is MCP?">
The [Model Context Protocol](https://modelcontextprotocol.io/) is an open standard for connecting AI agents to external tools and data sources. Each server below exposes one or more tools that agents can invoke during a session.
</Aside>

<Aside type="note" title="Source of truth">
**Authoring SSOT:** `config/mcp-registry.json` (67 top-level server keys). Edit once there, regenerate MCPHub settings, and project clients via stack sync. Root `mcp.json` is an MCPHub **projection** (group/server proxies + bearer), not the catalog authoring surface. First-party packages live under `mcp/<name>/` with matching docs at `/mcp/<name>/`.
</Aside>

<Aside type="caution" title="Candidate runtime overlay">
The July 2026 corpus verifies 17 pinned MCP artifacts. All are disabled in the registry and excluded from default groups; configuration and package presence do not imply activation. See `planning/manifests/candidate-corpus-jul2026/non-skill-install-assurance.json` for per-target pins, auth variable names, probe method, and activation state.
</Aside>

<Aside type="caution" title="Partial inventory below">
Category cards below are a human-oriented subset and may lag the full registry. For the complete machine SSOT see [MCP registry](/harness-config/mcp-registry/) and first-party pages under this section’s sidebar.
</Aside>

## First-party repo MCP

<CardGrid>
  <LinkCard title="MCPHub" href="/mcp/mcphub/" description="Local control plane package and launch docs." />
  <LinkCard title="agent-catalog" href="/mcp/agent-catalog/" description="Portable agent catalog MCP." />
  <LinkCard title="skill-catalog" href="/mcp/skill-catalog/" description="Skills catalog MCP." />
  <LinkCard title="docs-index" href="/mcp/docs-index/" description="Docs search / index MCP." />
  <LinkCard title="docs-graph" href="/mcp/docs-graph/" description="Docs graph MCP." />
  <LinkCard title="Registry publisher" href="/mcp/registry-publisher/" description="Catalog publish helpers." />
</CardGrid>

## Managed client sync coverage

<Aside type="tip" title="Sync source of truth">
Edit `config/mcp-registry.json`, then preview local drift with `uv run python scripts/sync_agent_stack.py --targets home --check`. Apply repo and home updates with `uv run python scripts/sync_agent_stack.py --targets repo,home --apply`.
</Aside>

<Aside type="note" title="MCPHub local control plane">
MCPHub is now the preferred local MCP control plane. When `mcphub.enabled` is true in `config/mcp-registry.json`, clients receive MCPHub all, group, server, and disabled Smart Routing endpoints rooted at `http://127.0.0.1:46683`. Start it locally with `just mcphub-up`; Docker is not used for the managed launch path. ChatGPT remote MCP access uses the opt-in Cloudflare Tunnel URL from `.env.mcphub`.
</Aside>

<Aside type="note" title="Exact vs merge behavior">
`ChatGPT Desktop`, the GitHub Copilot shadow config, `Gemini antigravity`, the Gemini extension config, `OpenCode`, `AITK`, and `Crush` all receive the canonical baseline **and** preserve non-canonical extras already present in those files. `Claude Desktop`, `Cursor`, `Gemini CLI`, `GitHub Copilot CLI`, and `Codex` are rewritten to the exact canonical baseline. `Cherry Studio` receives a generated import pack in a tool-owned subdirectory instead of direct config mutation so unmanaged local MCP entries and ad hoc import bundles in the parent directory stay untouched.
</Aside>

| Client | Managed path | Root key / shape | Sync behavior |
| --- | --- | --- | --- |
| ChatGPT Desktop | `~/Library/Application Support/ChatGPT/mcp.json` | `mcpServers` | Canonical baseline + preserve non-canonical extras |
| Claude Code local enablement | `~/.claude/settings.local.json` | `enabledMcpjsonServers` | Filters enabled names to valid synced servers |
| Claude Desktop | `~/Library/Application Support/Claude/claude_desktop_config.json` | `mcpServers` | Exact canonical baseline |
| Cherry Studio | `~/Library/Application Support/CherryStudio/mcp-import/managed/` | `all.json` + one `<server>.json` per managed server | Generated import pack; does not mutate live app state |
| Codex | `~/.codex/config.toml` | `[mcp_servers.<name>]` | Exact canonical baseline |
| Crush | `~/.config/crush/crush.json` | `mcp` | Canonical baseline + preserve non-canonical extras |
| Cursor | `~/.cursor/mcp.json` | `mcpServers` | Exact canonical baseline |
| Gemini CLI | `~/.gemini/settings.json` | `mcpServers` | Exact canonical baseline |
| Gemini antigravity | `~/.gemini/antigravity/mcp_config.json` | `mcpServers` | Canonical baseline + preserve non-canonical extras |
| Gemini extension antigravity | `~/.gemini/extensions/outline-driven-development/antigravity/mcp_config.json` | `mcpServers` | Canonical baseline + preserve extension-specific extras |
| GitHub Copilot CLI | `~/.copilot/mcp-config.json` | `mcpServers` | Exact canonical baseline with Copilot tool allowlists |
| GitHub Copilot shadow config | `~/.config/.copilot/mcp-config.json` | `mcpServers` | Canonical baseline + preserve non-canonical extras in Copilot-style schema |
| LM Studio | `~/.lmstudio/mcp.json` | `mcpServers` | MCPHub remote-stdio by default; canonical managed entries + preserve non-canonical extras |
| OpenCode | `~/.config/opencode/opencode.json` | `mcp` | Canonical baseline + preserve non-canonical extras in local/remote schema |
| AITK | `~/.aitk/mcp.json` | `servers` | Canonical baseline + preserve non-canonical extras |

<Steps>

1. Update the canonical MCP registry.

   Edit `config/mcp-registry.json` or the repo sources that feed it.

2. Preview repo and home drift.

   ```bash
   uv run python scripts/sync_agent_stack.py --targets repo,home --check
   ```

3. Apply the synchronized config set.

   ```bash
   uv run python scripts/sync_agent_stack.py --targets repo,home --apply
   ```

4. Import Cherry Studio MCP files only if you use Cherry Studio.

   Use `~/Library/Application Support/CherryStudio/mcp-import/managed/<server>.json` for additive one-server imports, or `~/Library/Application Support/CherryStudio/mcp-import/managed/all.json` only when you intentionally want to replace Cherry Studio's full managed MCP list.

   Top-level files already sitting in `~/Library/Application Support/CherryStudio/mcp-import/` are left alone intentionally.

</Steps>

## DuckDuckGo setup (representative client snippets)

Use server key `duckduckgo` with the `duckduckgo-mcp-server` package.

#### Standard JSON clients (`mcpServers`)

Applies to ChatGPT Desktop, Claude Desktop, Cursor, Gemini CLI, Gemini antigravity, and the Gemini extension config.

```json
{
  "mcpServers": {
    "duckduckgo": {
      "command": "uvx",
      "args": ["duckduckgo-mcp-server"]
    }
  }
}
```

#### Codex (`~/.codex/config.toml`)

```toml
[mcp_servers.duckduckgo]
command = "uvx"
args = ["duckduckgo-mcp-server"]
```

#### GitHub Copilot CLI (`~/.copilot/mcp-config.json`)

```json
{
  "mcpServers": {
    "duckduckgo": {
      "type": "stdio",
      "command": "uvx",
      "args": ["duckduckgo-mcp-server"],
      "tools": ["*"]
    }
  }
}
```

#### OpenCode (`~/.config/opencode/opencode.json`)

```json
{
  "mcp": {
    "duckduckgo": {
      "type": "local",
      "command": ["uvx", "duckduckgo-mcp-server"],
      "enabled": true
    }
  }
}
```

#### AITK (`~/.aitk/mcp.json`)

```json
{
  "servers": {
    "duckduckgo": {
      "type": "stdio",
      "command": "uvx",
      "args": ["duckduckgo-mcp-server"]
    }
  }
}
```

## Chrome DevTools setup (representative client snippets)

Use server key `chrome-devtools` through the managed MCPHub wrapper. The wrapper starts or reuses a dedicated visible Chrome on `127.0.0.1:9333` with profile `~/.cache/chrome-devtools-mcp-login`, then attaches `chrome-devtools-mcp` with `--browserUrl`. This avoids the automation launch flags that `chrome-devtools-mcp` passes when it launches Chrome itself. Chrome opens lazily when the client invokes a browser-backed tool such as `list_pages`, `navigate_page`, or `take_screenshot`; restart the MCP client after config changes so it does not keep using an already-started server process.

Replace `<repo>` in the examples below with the absolute path to your local checkout.

#### Standard JSON clients (`mcpServers`)

Applies to ChatGPT Desktop, Claude Desktop, Cursor, Gemini CLI, Gemini antigravity, and the Gemini extension config.

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "bash",
      "args": ["<repo>/scripts/mcphub/chrome-devtools-browser-url.sh"]
    }
  }
}
```

#### Codex (`~/.codex/config.toml`)

```toml
[mcp_servers.chrome-devtools]
command = "bash"
args = ["<repo>/scripts/mcphub/chrome-devtools-browser-url.sh"]
```

#### GitHub Copilot CLI (`~/.copilot/mcp-config.json`)

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "type": "stdio",
      "command": "bash",
      "args": ["<repo>/scripts/mcphub/chrome-devtools-browser-url.sh"],
      "tools": ["*"]
    }
  }
}
```

#### OpenCode (`~/.config/opencode/opencode.json`)

```json
{
  "mcp": {
    "chrome-devtools": {
      "type": "local",
      "command": ["bash", "<repo>/scripts/mcphub/chrome-devtools-browser-url.sh"],
      "enabled": true
    }
  }
}
```

#### Crush (`~/.config/crush/crush.json`)

```json
{
  "mcp": {
    "chrome-devtools": {
      "type": "stdio",
      "command": "bash",
      "args": ["<repo>/scripts/mcphub/chrome-devtools-browser-url.sh"]
    }
  }
}
```

---

## Thinking and Reasoning (10)

> Structured reasoning frameworks that guide agents through multi-step analysis, creative problem-solving, and decision evaluation.

<CardGrid>
  <Card title="sequential-thinking">
    <Badge text="Analysis" variant="note" /> Step-by-step reasoning with revision and branching. The reference implementation from Anthropic for dynamic, reflective thought processes.
  </Card>
  <Card title="atom-of-thoughts">
    <Badge text="Synthesis" variant="note" /> Decomposes complex problems into atomic, independent thought units that can be contracted and combined for robust answers.
  </Card>
  <Card title="shannon-thinking">
    <Badge text="Information Theory" variant="note" /> Information-theoretic reasoning inspired by Claude Shannon. Reduces noise and uncertainty through systematic problem decomposition.
  </Card>
  <Card title="structured-thinking">
    <Badge text="Memory" variant="note" /> Captures, retrieves, and revises thoughts with persistent history. Supports tagging and relevance-based retrieval across sessions.
  </Card>
  <Card title="cascade-thinking">
    <Badge text="Deep Dive" variant="note" /> Multi-stage reasoning cascade that escalates through progressively deeper analysis levels when simpler passes are insufficient.
  </Card>
  <Card title="crash">
    <Badge text="Verification" variant="note" /> Constraint-satisfaction reasoning with strict mode toggle. Enforces logical consistency checking on intermediate conclusions.
  </Card>
  <Card title="deep-lucid-3d">
    <Badge text="Exploration" variant="note" /> Three-dimensional problem analysis exploring depth, breadth, and lateral connections. Includes creative exploration mode.
  </Card>
  <Card title="lotus-wisdom-mcp">
    <Badge text="Perspective" variant="note" /> Eastern-philosophy-inspired reasoning combining multiple perspectives and reflective synthesis for balanced conclusions.
  </Card>
  <Card title="think-strategies">
    <Badge text="Routing" variant="note" /> Strategy-selection toolkit that matches reasoning patterns to problem types. Session management for ongoing analysis threads.
  </Card>
  <Card title="creative-thinking">
    <Badge text="Ideation" variant="note" /> Lateral and divergent thinking techniques for brainstorming, ideation, and breaking out of conventional solution spaces.
  </Card>
</CardGrid>

---

## Search and Discovery (8)

> Web search engines and content discovery services for retrieving up-to-date information from the internet.

<CardGrid>
  <Card title="brave-search">
    <Badge text="Web/Local" variant="tip" /> Brave Search API with web, news, image, video, and local search. Includes AI-powered summarization of search results.
  </Card>
  <Card title="exa">
    <Badge text="Neural Search" variant="tip" /> Neural search engine that understands meaning, not just keywords. Optimized for finding technical content, papers, and documentation.
  </Card>
  <Card title="g-search">
    <Badge text="Google" variant="tip" /> Google Search results via SerpAPI. Familiar ranking and broad coverage across the public web.
  </Card>
  <Card title="duckduckgo">
    <Badge text="Privacy First" variant="tip" /> Privacy-focused web search via DuckDuckGo. No tracking, no API key required.
  </Card>
  <Card title="ddgs">
    <Badge text="Metasearch" variant="tip" /> Multi-engine text, image, news, video, book, and extraction tools via DDGS. No API key. Use snippet search by default; `extract_content` is high-latency and token-heavy.
  </Card>
  <Card title="arxiv">
    <Badge text="Academic" variant="tip" /> arXiv paper search and metadata retrieval for research workflows, including direct access to paper IDs and summaries.
  </Card>
  <Card title="wikipedia">
    <Badge text="Encyclopedic" variant="tip" /> Wikipedia content retrieval and lookup tools for encyclopedic background, entities, and topic overviews.
  </Card>
  <Card title="wayback">
    <Badge text="Archive" variant="tip" /> Access archived pages from the Internet Archive Wayback Machine for historical snapshots and link recovery.
  </Card>
  <Card title="tavily">
    <Badge text="Research" variant="tip" /> Tavily-powered web search, extraction, crawl, and deep-research workflows for current-event and multi-source investigations.
  </Card>
</CardGrid>

---

## Web Fetching and Extraction (4)

> Tools for retrieving, rendering, and extracting structured content from web pages and URLs.

<CardGrid>
  <Card title="fetch">
    <Badge text="HTTP Fetch" variant="caution" /> Core URL fetcher from the MCP reference servers. Retrieves web pages and returns content in multiple formats.
  </Card>
  <Card title="fetcher">
    <Badge text="JS Rendering" variant="caution" /> Browser-based URL fetcher with JavaScript rendering. Handles SPAs and dynamic content that simple HTTP fetches miss.
  </Card>
  <Card title="trafilatura">
    <Badge text="Content Extraction" variant="caution" /> High-quality web content extraction using the Trafilatura library. Strips boilerplate to return clean article text and metadata.
  </Card>
  <Card title="scrapling">
    <Badge text="Opt-in Scraping" variant="caution" /> CSS-targeted web scraping with stealth HTTP fetch and optional headless browser sessions. Opt-in via MCPHub `research`, `web-read`, `live-browser`, and related groups; not in default `harness`. Run `scrapling install` for browser tools.
  </Card>
  <Card title="reddit-mcp-buddy">
    <Badge text="Opt-in Reddit" variant="caution" /> Read-only Reddit browse/search/user tools via `reddit-mcp-buddy@1.1.13`. Opt-in via MCPHub `research`, `shared-read`, and `experimental`; not in default `harness` or `tunnel`. Anonymous works with no credentials; optional OAuth elevation uses local `.env.mcphub` only. Treat Reddit content as untrusted evidence.
  </Card>
</CardGrid>

---

## Knowledge and Documentation (3)

> Access library documentation, open-source wiki knowledge, and codebase analysis tools.

<CardGrid>
  <Card title="context7">
    <Badge text="API Docs" variant="success" /> Retrieves up-to-date documentation for libraries and frameworks directly from source. Eliminates hallucinated APIs and outdated references.
  </Card>
  <Card title="deepwiki">
    <Badge text="GitHub Wikis" variant="success" /> Queries the DeepWiki knowledge base of open-source project documentation. Ask questions about repos and read generated wiki pages.
  </Card>
  <Card title="repomix">
    <Badge text="Codebase Context" variant="success" /> Packs entire codebases into AI-friendly context. Supports local directories and remote repositories with grep and directory browsing.
  </Card>
</CardGrid>

---

## Notebooks (1)

> Live Jupyter notebook management and kernel execution. Requires a user-owned JupyterLab instance and opt-in MCPHub groups (`notebooks`, `coding`, `heavy`, etc.); not in default `harness`.

<CardGrid>
  <Card title="jupyter-mcp-server">
    <Badge text="Opt-in Notebooks" variant="caution" /> Datalayer Jupyter MCP: list/read notebooks and cells, edit cells, execute code in kernels, multimodal outputs. Connect via MCPHub `/mcp/notebooks` or workflow groups; set `JUPYTER_URL` and `JUPYTER_TOKEN` in `.env.mcphub`.
  </Card>
</CardGrid>

---

## Development Tools (3)

> Browser automation, package management, and document processing for development workflows.

<CardGrid>
  <Card title="chrome-devtools">
    <Badge text="CDP Automation" variant="danger" /> Chrome DevTools Protocol automation via Puppeteer. Navigate, click, fill forms, trace performance, inspect network requests, and take screenshots. Requires Node.js v22+.
  </Card>
  <Card title="package-version-check-mcp">
    <Badge text="Dependency Mgmt" variant="danger" /> Checks latest stable versions across npm, PyPI, Go, Maven, Docker, Helm, Terraform, GitHub Actions, and mise-backed devtools via PyPI `uvx`.
  </Card>
  <Card title="docling">
    <Badge text="Doc Processing" variant="danger" /> Converts documents (PDF, DOCX, PPTX, HTML) into structured Docling format. Create, edit, search, and export documents as Markdown.
  </Card>
</CardGrid>

---

## Productivity (1)

> Email, task management, and personal productivity integrations.

<CardGrid>
  <Card title="gmail">
    <Badge text="Inbox Zero" variant="default" /> Full Gmail access: search, read, send, draft, label, filter, and batch-manage emails. Supports attachment downloads.
  </Card>
</CardGrid>

---

## Social (1)

> Social media and professional network integrations.

<CardGrid>
  <Card title="linkedin">
    <Badge text="Professional Data" variant="tip" /> LinkedIn profile scraping for professional networking data. Extract public profile information and connections.
  </Card>
</CardGrid>
