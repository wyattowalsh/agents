---
title: "image-input-optimizer-guard"
description: "Resize and compress oversized local image inputs before harnesses consume them."
page_kind: "hook"
source_kind: "registry"
asset_id: "image-input-optimizer-guard"
composed: true
composed_at: "2026-07-10"
composed_by: "regenerate-hook-docs-rv-006"
docs_density: standard
---

{/* GENERATED from config/hook-registry.json — do not hand-edit */}

import { Badge, CardGrid, LinkCard } from '@astrojs/starlight/components';

<Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> <Badge text="codex" variant="default" /> <Badge text="claude-code" variant="default" /> <Badge text="cursor" variant="default" />

> Resize and compress oversized local image inputs before harnesses consume them.

## Registry Entry

| Field | Value |
|-------|-------|
| id | `image-input-optimizer-guard` |
| logical_event | `PreToolUse` |
| matcher | `analyze_image|attach_image|screenshot|upload_image|view_image|Read|read` |
| command | `{hook_runner} image-input-optimizer-guard --harness {harness}` |
| timeout | `60` |
| mode | `enforce` |
| harnesses | `[
  "codex",
  "claude-code",
  "cursor"
]` |
| bundle_group | `fleet-pre-tool-enforce` |
| bundle_mode | `enforce-chain` |
| status_message | Optimizing image inputs |
| degraded_behavior | block with an optimized retry path when the harness cannot safely accept updated input |

<details class="source-disclosure">
<summary>Full hook config (registry SSOT)</summary>

```json title="config/hook-registry.json (entry)"
{
  "id": "image-input-optimizer-guard",
  "description": "Resize and compress oversized local image inputs before harnesses consume them.",
  "logical_event": "PreToolUse",
  "logical_policy": "image-input-optimizer-guard",
  "mode": "enforce",
  "status_message": "Optimizing image inputs",
  "matcher": "analyze_image|attach_image|screenshot|upload_image|view_image|Read|read",
  "command": "{hook_runner} image-input-optimizer-guard --harness {harness}",
  "timeout": 60,
  "bundle_group": "fleet-pre-tool-enforce",
  "bundle_mode": "enforce-chain",
  "degraded_behavior": "block with an optimized retry path when the harness cannot safely accept updated input",
  "harnesses": [
    "codex",
    "claude-code",
    "cursor"
  ]
}
```

Command placeholders:

- `{hook_runner}` resolves to the fleet dispatcher (`hooks/wagents-hook.py` or harness projection).
- `{harness}` is the target harness id at render/install time.

</details>

## Related

<CardGrid>
  <LinkCard title="Hooks hub" href="/hooks/" description="Registry overview and harness matrix." />
  <LinkCard title="CLI hooks" href="/cli/" description="`wagents hooks list|validate`." />
</CardGrid>
