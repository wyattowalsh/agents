---
title: "Hooks"
description: "Repo-managed lifecycle hooks for agent harnesses (Claude, Codex, Copilot, Gemini, etc)."
page_kind: "hooks-hub"
source_kind: "registry"
composed: true
composed_at: "2026-06-16"
composed_by: "compose-hooks-wave-1"
---

{/* HAND-MAINTAINED */}

import { Card, CardGrid, Badge, Aside, LinkCard } from '@astrojs/starlight/components';

> Repo-managed, portable lifecycle hooks declared in `config/hook-registry.json`. These provide safety, context, formatting, lint, research guards, and audit surfaces across harnesses. Hand-maintained catalog pages (composed).

<Badge text="32 hooks" variant="note" size="large" />

<Aside type="tip" title="Source of truth">
All hooks are defined in `config/hook-registry.json`. Use `wagents hooks list` and `wagents hooks validate` to inspect at runtime. Individual hook pages live under `/hooks/<id>/`.
</Aside>

## Core (github-copilot baseline)

These run for the GitHub Copilot harness via direct shell handlers.

<CardGrid>
  <Card title="session-start">
    <Badge text="SessionStart" variant="success" /> Report basic session environment and repository state (branch, dirty files, tool presence).
  </Card>
  <Card title="prompt-log">
    <Badge text="UserPromptSubmit" variant="success" /> Local prompt audit logging (short retention) for Copilot sessions.
  </Card>
  <Card title="destructive-shell-guard">
    <Badge text="PreToolUse" variant="caution" /> Block rm -rf on critical paths, remote-pipe-to-shell, force-push to main, git reset --hard, git clean -f.
  </Card>
  <Card title="protected-file-guard">
    <Badge text="PreToolUse" variant="caution" /> Deny edits to secrets (.env*, credentials, tokens), .git internals, lockfiles, and path traversal.
  </Card>
  <Card title="post-edit-format">
    <Badge text="PostToolUse" variant="success" /> Best-effort auto-format for py (ruff), js/ts (prettier), rs, go after writes/edits.
  </Card>
  <Card title="post-edit-lint">
    <Badge text="PostToolUse" variant="success" /> Return concise Ruff/Ty or tsc issues as additionalContext after edits (non-blocking).
  </Card>
</CardGrid>

## Codex-specific

Codex harness uses the Python dispatcher (`wagents-hook.py`) for richer policy + status messages. All are registry-sourced.

<CardGrid>
  <Card title="codex-session-start-context">
    <Badge text="SessionStart" variant="success" /> <Badge text="context" variant="tip" /> Lightweight git + repo context for Codex sessions.
  </Card>
  <Card title="codex-destructive-shell-guard">
    <Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> Critical shell blocks tailored for Codex payloads.
  </Card>
  <Card title="codex-protected-file-guard">
    <Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> Expanded matcher covering writes + shell for Codex protected-path enforcement.
  </Card>
  <Card title="codex-permission-request-guard">
    <Badge text="PermissionRequest" variant="caution" /> <Badge text="enforce" variant="tip" /> Deny high-risk permission requests while preserving normal approval flow.
  </Card>
  <Card title="codex-post-tool-verify-context">
    <Badge text="PostToolUse" variant="success" /> <Badge text="context" variant="tip" /> Lightweight quality checks + guidance injected as context (no block).
  </Card>
  <Card title="codex-stop-truth-gate">
    <Badge text="Stop" variant="caution" /> <Badge text="enforce" variant="tip" /> Require validation evidence before allowing stop when code-change claims are present.
  </Card>
</CardGrid>

## Cursor-specific

Cursor uses the Python dispatcher (`wagents-hook.py`) plus its own native lifecycle events (`beforeReadFile`, `beforeShellExecution`, `beforeMCPExecution`, `afterFileEdit`, `subagentStart`). Enforce-tier native events are rendered fail-closed.

<CardGrid>
  <Card title="cursor-session-start-context">
    <Badge text="SessionStart" variant="success" /> <Badge text="context" variant="tip" /> Lightweight Cursor session + repo context.
  </Card>
  <Card title="cursor-destructive-shell-guard">
    <Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> Block critical destructive shell commands in Cursor.
  </Card>
  <Card title="cursor-protected-file-guard">
    <Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> Block protected files, secret paths, git internals, traversal, and lockfile edits.
  </Card>
  <Card title="cursor-post-tool-verify-context">
    <Badge text="PostToolUse" variant="success" /> <Badge text="context" variant="tip" /> Lightweight post-edit quality checks + verification guidance.
  </Card>
  <Card title="cursor-stop-truth-gate">
    <Badge text="Stop" variant="caution" /> <Badge text="enforce" variant="tip" /> Ask for one more pass when final claims omit validation evidence.
  </Card>
  <Card title="cursor-before-read-file-guard">
    <Badge text="BeforeReadFile" variant="caution" /> <Badge text="enforce" variant="tip" /> Block reads of secret-bearing files before they enter Cursor context.
  </Card>
  <Card title="cursor-before-shell-execution-guard">
    <Badge text="BeforeShellExecution" variant="caution" /> <Badge text="enforce" variant="tip" /> Block destructive shell + unsafe git history rewrites pre-execution.
  </Card>

> **Shell guard layering:** `cursor-destructive-shell-guard` on `preToolUse` and `cursor-before-shell-execution-guard` on `beforeShellExecution` intentionally overlap for defense-in-depth — legacy preToolUse coverage plus native pre-execution hooks (including cloud agents that lack `stop`).
  <Card title="cursor-before-mcp-execution-guard">
    <Badge text="BeforeMCPExecution" variant="caution" /> <Badge text="enforce" variant="tip" /> Block destructive MCP tool calls before execution.
  </Card>
  <Card title="cursor-after-file-edit-context">
    <Badge text="AfterFileEdit" variant="success" /> <Badge text="context" variant="tip" /> Lightweight post-edit quality context after file edits.
  </Card>
  <Card title="cursor-subagent-start-context">
    <Badge text="SubagentStart" variant="success" /> <Badge text="context" variant="tip" /> Inject repo conventions + session context when a subagent starts.
  </Card>
</CardGrid>

## Cross-harness safety & optimization

Tier B policies that span codex, claude-code, cursor, github-copilot, and gemini-cli via the shared dispatcher. Grok consumes them through the fleet deny adapter (`~/.grok/hooks/wagents-fleet.json`).

<CardGrid>
  <Card title="git-commit-push-guard">
    <Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> Block force-pushes to protected branches, hook-skipping commits, and unsafe history rewrites.
  </Card>
  <Card title="image-input-optimizer-guard">
    <Badge text="PreToolUse" variant="caution" /> <Badge text="enforce" variant="tip" /> Resize + compress oversized local image inputs before harnesses consume them.
  </Card>
</CardGrid>

## Claude-specific

Claude Code uses Stop / SubagentStop quality gates wired through `.claude/settings.json`.

<CardGrid>
  <Card title="verify-before-stop">
    <Badge text="Stop" variant="caution" /> <Badge text="enforce" variant="tip" /> Block stopping when modified files carry debug artifacts or fail fast lint/type checks.
  </Card>
  <Card title="task-completed-gate">
    <Badge text="Stop" variant="caution" /> <Badge text="enforce" variant="tip" /> Block completion on syntax errors, incomplete markers, or failing tests.
  </Card>
  <Card title="teammate-idle-gate">
    <Badge text="SubagentStop" variant="caution" /> <Badge text="enforce" variant="tip" /> Block a subagent from going idle with broken syntax or critical failures.
  </Card>
</CardGrid>

## Research guards (multi-harness)

Activated by research prompts. Span **codex, claude-code, cursor, github-copilot, gemini-cli**. Use state machine + ledger for invariants. Detail pages: `/hooks/<id>/`.

<CardGrid>
  <LinkCard title="research-prompt-triage-context" href="/hooks/research-prompt-triage-context/" description="UserPromptSubmit · context — Detects research intent, activates state, injects method reminders." />
  <LinkCard title="research-readonly-write-guard" href="/hooks/research-readonly-write-guard/" description="PreToolUse · enforce — Blocks source writes while research state is active (degraded=audit)." />
  <LinkCard title="research-dangerous-shell-guard" href="/hooks/research-dangerous-shell-guard/" description="PreToolUse · enforce — Blocks dangerous shell during research (degraded=audit)." />
  <LinkCard title="research-evidence-ledger" href="/hooks/research-evidence-ledger/" description="PostToolUse · audit — Appends provenance to the per-session ledger." />
  <LinkCard title="research-stop-verifier" href="/hooks/research-stop-verifier/" description="Stop · enforce — Final invariant checks before allowing research session stop." />
</CardGrid>

## Dispatcher & Implementation Notes

- GitHub Copilot direct `.sh` handlers under `./hooks/`.
- Codex, Cursor + multi-harness policies use `hooks/wagents-hook.py <policy-id> --harness <name>`. Payload normalization, state, ledger, and degraded modes live here.
- Cursor native events (`beforeReadFile`, `beforeShellExecution`, `beforeMCPExecution`, `afterFileEdit`, `subagentStart`) are rendered into the flat `.cursor/hooks.json` shape; enforce-tier guards are fail-closed.
- Decision logic is being migrated into pure modules under `wagents/hooks/policies/` (e.g. `git_commit_push_guard`, `before_read_file_guard`, `before_mcp_execution`, `subagent_start`, `grok_deny_adapter`) so the dispatcher stays thin and unit-testable.
- Grok consumes the dispatcher-backed fleet via a deny adapter (`render_grok_hooks` → `~/.grok/hooks/wagents-fleet.json`); OpenCode bridges enforce-tier PreToolUse guards through `platforms/opencode/plugins/wagents-hook-bridge.ts`.
- Research policies also delegate to `skills/research/scripts/research_hook.py` for the full state machine and stop verifier.
- All entries are validated by `wagents hooks validate` and `scripts/validate/validate_repo.py`.

## Runtime performance tiers

Fleet hook rendering supports staged performance tiers via `config/tooling-policy.json`:

```json
"hook_perf": { "tier": "bundle" }
```

| Tier | Behavior |
| --- | --- |
| `legacy` | One rendered entry per registry row — byte-identical to pre-performance baselines (rollback tier). |
| `g1` | Registry quick wins (matcher narrowing, dispatcher caches) without bundling. |
| `bundle` | Consecutive `bundle_group` rows collapse to a single `{hook_runner} --bundle ...` spawn. |
| `worker` | Bundle groups render through `hooks/wagents-hook-worker.py` (optional warm-process path). |

The worker daemon (`python3 hooks/wagents-hook-worker.py --serve --socket PATH`) handles **one socket connection at a time**: its accept loop serves each NDJSON request to completion before accepting the next client. Parallel harness hook events queue at the socket backlog; this is acceptable for enforce-chain bundle semantics. **Start the daemon and set `WAGENTS_HOOK_WORKER_SOCKET` to the socket path before** promoting `hook_perf.tier` to `worker` and running `sync_agent_stack.py --apply` — rendered commands forward through `wagents-hook.py --worker-socket` and will fail closed or fall back if the socket is absent.

Observability: set `WAGENTS_HOOK_TIMING=1` on a hook invocation to append JSON timing lines to `~/.cache/wagents/hook-timing.jsonl`. Worker-tier forwards (`--worker-socket`) record the same sidecar with `"forwarded": true` so hyperfine runs can distinguish warm socket paths from cold spawns. Bundle worker forwards use a socket wait budget of `bundle_timeout + 1s` (aligned with the OpenCode bridge margin). Inventory spawn counts with `uv run python scripts/hooks/hook_perf_inventory.py --json`.

The repo default tier is **`bundle`** after [Hooks Bundle Promotion v2](https://github.com/wyattowalsh/agents/tree/main/openspec/changes/fleet-hooks-promotion) (`openspec/changes/fleet-hooks-promotion/`). Committed spawn baselines live under `docs/public/generated-reports/hook-perf-baseline-*.json`. Maintainer workflow: `docs/runbooks/hook-performance.md`. Promote to `worker` only after hyperfine validation documented in `openspec/changes/fleet-hooks-performance/validation-matrix.md`.

## Related

- CLI: `wagents hooks list --format json`, `wagents hooks validate`
- Registry: `config/hook-registry.json`
- Safety surface also includes MCPHub groups and agent permissionMode settings.
- Research skill: `/skills/catalog/custom/research/`

<CardGrid>
  <LinkCard
    title="Install skills & harnesses"
    href="/install/"
    description="Portable install paths, wagents skills sync dry-run, and harness onboarding."
  />
  <LinkCard
    title="Harness config hub"
    href="/harness-config/"
    description="Sync manifest, MCP registry, RTK, and fleet projection surfaces."
  />
  <LinkCard
    title="MCPHub"
    href="/mcp/"
    description="Local MCP control plane, groups, and registry-driven server projection."
  />
  <LinkCard
    title="CLI reference"
    href="/cli/"
    description="wagents validate, hooks, docs generate, and maintainer commands."
  />
</CardGrid>

---
[View source on GitHub](https://github.com/wyattowalsh/agents/blob/main/config/hook-registry.json)
