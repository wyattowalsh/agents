---
title: "cursor-destructive-shell-guard"
description: "Block critical destructive shell commands in Cursor."
page_kind: "hook"
source_kind: "registry"
asset_id: "cursor-destructive-shell-guard"
composed: true
composed_at: "2026-06-23"
composed_by: "compose-hooks-cursor-wave-1"
docs_density: standard
---

{/* HAND-MAINTAINED */}

import { Badge, CardGrid, LinkCard, Aside } from '@astrojs/starlight/components';

<Badge text="hook" variant="note" /> <Badge text="PreToolUse" variant="caution" /> <Badge text="cursor" variant="default" /> <Badge text="mode: enforce" variant="tip" />

> Block critical destructive shell commands in Cursor.

## Registry Entry

| Field | Value |
|-------|-------|
| id | `cursor-destructive-shell-guard` |
| status_message | `Checking shell safety` |
| mode | `enforce` |
| logical_event | `PreToolUse` |
| matcher | `Bash\|bash\|run_shell_command\|shell\|terminal` |
| command | `python3 {repo_root}/hooks/wagents-hook.py cursor-destructive-shell-guard --harness {harness}` |
| timeout | `5` |
| harnesses | `["cursor"]` |

<Aside type="note">
Shared dispatcher `wagents-hook.py` with `_policy_codex_destructive_shell_guard` (re-uses core destructive patterns, emits Cursor-specific deny JSON).
</Aside>

<details class="source-disclosure">
<summary>Full hook config + command reference</summary>

```json title="config/hook-registry.json (entry)"
{
  "id": "cursor-destructive-shell-guard",
  "description": "Block critical destructive shell commands in Cursor.",
  "status_message": "Checking shell safety",
  "mode": "enforce",
  "logical_event": "PreToolUse",
  "matcher": "Bash|bash|run_shell_command|shell|terminal",
  "command": "python3 {repo_root}/hooks/wagents-hook.py cursor-destructive-shell-guard --harness {harness}",
  "timeout": 5,
  "harnesses": [
    "cursor"
  ]
}
```

```python title="hooks/wagents-hook.py (policy)"
def _policy_codex_destructive_shell_guard(payload: NormalizedPayload) -> int:
    if _tool_name(payload) not in SHELL_TOOL_NAMES and not payload.command:
        return 0
    reason = _destructive_shell_reason(payload.command)
    if reason:
        return _deny(payload, reason, policy_id="cursor-destructive-shell-guard")
    return 0
```

Command: `python3 .../wagents-hook.py cursor-destructive-shell-guard --harness cursor`
</details>

## Resources

<CardGrid>
  <LinkCard title="Hooks Hub" href="/hooks/" description="All repo-managed lifecycle hooks." />
  <LinkCard title="wagents hooks" href="/cli/" description="Inspect and validate hooks via CLI." />
</CardGrid>

---
[View source on GitHub](https://github.com/wyattowalsh/agents/blob/main/config/hook-registry.json)
