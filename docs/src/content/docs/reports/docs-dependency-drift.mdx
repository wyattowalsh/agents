---
title: Docs Dependency Drift
description: Drift between docs npm dependencies, astro.config.mjs plugin wiring, and the docs artifact registry
---

{/* Auto-generated by wagents docs generate - do not edit */}

**Status:** 🟢 No drift detected

Checks that every `starlight-*` npm dependency in `docs/package.json` is either registered as a plugin in `docs/astro.config.mjs` or explicitly accounted in `config/docs-artifact-registry.json`, and that every declared artifact/source path still exists on disk.

## Starlight plugin dependencies

- `starlight-announcement` ✅
- `starlight-auto-sidebar` ✅ accounted — blocked because wagents docs generate owns sidebar generation
- `starlight-base-path` ✅ accounted — blocked because the site is root-deployed on Vercel
- `starlight-blog` ✅ accounted — blocked until a blog collection and route taxonomy exist
- `starlight-changelogs` ✅ accounted — blocked until changelog collection entries and release taxonomy exist
- `starlight-codeblock-fullscreen` ✅
- `starlight-cooler-credit` ✅ accounted — blocked because it owns pagination and table-of-contents slots
- `starlight-docsearch-typesense` ✅ accounted — blocked until Typesense credentials and search ownership are configured
- `starlight-dot-md` ✅ accounted — blocked because it rewrites content output paths
- `starlight-fullview-mode` ✅ accounted — blocked by the narrower enabled starlight-codeblock-fullscreen owner
- `starlight-github-alerts` ✅
- `starlight-heading-badges` ✅ accounted — blocked because it owns table-of-contents slots
- `starlight-image-zoom` ✅
- `starlight-kbd` ✅
- `starlight-latest-version` ✅ accounted — blocked by its Astro 6 peer range against this Astro 7 site
- `starlight-links-validator` ✅
- `starlight-llm-button` ✅ accounted — blocked because it owns table-of-contents slots
- `starlight-llms-txt` ✅
- `starlight-markdown-blocks` ✅
- `starlight-obsidian` ✅ accounted — blocked until a vault import policy and content source exist
- `starlight-openapi` ✅ accounted — blocked until an OpenAPI source and route ownership exist
- `starlight-package-managers` ✅ accounted — used directly as an MDX component, not an Astro integration
- `starlight-page-actions` ✅
- `starlight-page-context-action` ✅ accounted — blocked because it owns sidebar and table-of-contents slots
- `starlight-plugin-icons` ✅
- `starlight-save-file-component` ✅ accounted — used directly as an MDX component, not an Astro integration
- `starlight-scroll-to-top` ✅
- `starlight-showcases` ✅ accounted — blocked until showcase data exists
- `starlight-sidebar-topics` ✅ accounted — blocked because wagents docs generate owns sidebar generation
- `starlight-sidebar-topics-dropdown` ✅ accounted — companion component blocked with starlight-sidebar-topics
- `starlight-site-graph` ✅
- `starlight-tags` ✅
- `starlight-theme-black` ✅
- `starlight-theme-obsidian` ✅ accounted — blocked because starlight-theme-black owns the active global theme
- `starlight-to-pdf` ✅ accounted — blocked behind explicit PDF export commands because it depends on Puppeteer
- `starlight-ui-tweaks` ✅ accounted — blocked because it owns theme, social, table-of-contents, markdown, and footer slots
- `starlight-versions` ✅ accounted — blocked until versioned content directories and route policy exist
- `starlight-videos` ✅ accounted — blocked until a video catalog and content policy exist
- `starlight-view-modes` ✅ accounted — blocked because it owns search, social, table-of-contents, and page-title slots

## Missing registered artifacts

_All artifacts declared in `config/docs-artifact-registry.json` exist on disk._

## Missing source registries

_All declared source registries exist on disk._
