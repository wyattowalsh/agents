---
title: "skill-eval-scaffolder"
description: "Scaffold evals/evals.json manifests for repo skills with baseline cases. Use when adding behavioral eval coverage. NOT for live LLM eval execution."
---

import { Badge, Tabs, TabItem, Card, CardGrid, LinkCard, Aside, Steps } from '@astrojs/starlight/components';

import SkillPageHeader from '../../../../../components/SkillPageHeader.astro';

<SkillPageHeader
  name="skill-eval-scaffolder"
  description="Scaffold evals/evals.json manifests for repo skills with baseline cases. Use when adding behavioral eval coverage. NOT for live LLM eval execution."
  installCommand={`npx skills add github:wyattowalsh/agents --skill skill-eval-scaffolder -y -g --agent antigravity --agent claude-code --agent codex --agent crush --agent cursor --agent gemini-cli --agent github-copilot --agent grok --agent opencode`}
  installLabel="Install"
  usage="/skill-eval-scaffolder &lt;skill-name&gt; [--apply]"
  badges={[{ text: "skill-eval-scaffolder", variant: "note" }, { text: "208 words", variant: "default" }, { text: "MIT", variant: "success" }, { text: "v1.0.0", variant: "tip" }, { text: "Repo-owned", variant: "tip" }]}
/>

{/* Quick Start is rendered by SkillPageHeader. */}

Works with [Claude Code](https://docs.anthropic.com/en/docs/claude-code), [Gemini CLI](https://github.com/google-gemini/gemini-cli), [OpenCode](https://github.com/anomalyco/opencode), and other [agentskills.io](https://agentskills.io)-compatible agents.

<Aside type="note" title="Source & provenance">
This page is generated from the repository file `skills/skill-eval-scaffolder/SKILL.md` by `wagents docs generate`.
</Aside>

## What It Does

Create minimal, schema-valid eval manifests for repository skills.

## Modes

| $ARGUMENTS | Action |
|------------|--------|
| `&lt;name&gt;` | Preview scaffold for `skills/&lt;name&gt;/evals/evals.json` |
| `&lt;name&gt; --apply` | Write scaffold when file is missing or user approved overwrite |
| `check &lt;name&gt;` | Validate an existing eval manifest |
| Empty | Show usage gallery and manifest schema summary |

## Critical Rules

1. Never overwrite an existing eval manifest without explicit user approval.
2. Include at least one explicit-invocation case in new scaffolds.
3. Run `validate_evals.py` before declaring the scaffold complete.
4. Route live eval execution to skill-creator `eval` mode or maintainer CI workflows.
5. Keep prompts grounded in the skill dispatch table and NOT-for boundaries.

<Tabs>
  <TabItem label="Public Metadata">
| Field | Value |
| ----- | ----- |
| Source Type | `repo-owned` |
| Display Source | `github:wyattowalsh/agents` |
| Source Kind | `repo` |
| Installability | portable command |
| Review State | reviewed |
| Target Agents | `antigravity`, `claude-code`, `codex`, `crush`, `cursor`, `gemini-cli`, `github-copilot`, `grok`, `opencode` |
  </TabItem>
  <TabItem label="General">
| Field | Value |
| ----- | ----- |
| Name | `skill-eval-scaffolder` |
| License | MIT |
| Version | 1.0.0 |
| Author | wyattowalsh |
  </TabItem>
  <TabItem label="Claude Code">
| Field | Value |
| ----- | ----- |
| Argument Hint | `[skill-name] [--apply]` |
  </TabItem>
</Tabs>

<details class="source-disclosure">
<summary>View Full SKILL.md</summary>

````yaml
---
name: skill-eval-scaffolder
description: >-
  Scaffold evals/evals.json manifests for repo skills with baseline cases.
  Use when adding behavioral eval coverage. NOT for live LLM eval execution.
user-invocable: true
argument-hint: "<skill-name> [--apply]"
license: MIT
metadata:
  author: wyattowalsh
  version: "1.0.0"
---

# Skill Eval Scaffolder

Create minimal, schema-valid eval manifests for repository skills.

**Scope:** Eval scaffolding only. Do not run live behavioral evals unless the maintainer explicitly approves.

## Dispatch

| $ARGUMENTS | Action |
|------------|--------|
| `<name>` | Preview scaffold for `skills/<name>/evals/evals.json` |
| `<name> --apply` | Write scaffold when file is missing or user approved overwrite |
| `check <name>` | Validate an existing eval manifest |
| Empty | Show usage gallery and manifest schema summary |

## Workflow

1. Confirm `skills/<name>/SKILL.md` exists.
2. Run the bundled scaffold script:

```bash
uv run python skills/skill-eval-scaffolder/scripts/scaffold_evals.py <name>
```

3. Add explicit, implicit, and negative-control cases before claiming adequate coverage.
4. Validate with the bundled check script.

## Manifest Shape

Each `evals/evals.json` uses:

- `skill_name` — kebab-case directory name
- `evals` — non-empty list of cases with `id`, `prompt`, `expected_output`, optional `files`, `assertions`

## Validation Contract

```bash
uv run python skills/skill-eval-scaffolder/scripts/check.py
```

## Critical Rules

1. Never overwrite an existing eval manifest without explicit user approval.
2. Include at least one explicit-invocation case in new scaffolds.
3. Run `validate_evals.py` before declaring the scaffold complete.
4. Route live eval execution to skill-creator `eval` mode or maintainer CI workflows.
5. Keep prompts grounded in the skill dispatch table and NOT-for boundaries.

````

[Download from GitHub](https://raw.githubusercontent.com/wyattowalsh/agents/main/skills/skill-eval-scaffolder/SKILL.md)

</details>

## Resources

<CardGrid>
  <LinkCard title="Skill Catalog" href="/skills/catalog/" description="Browse custom and external skills." />
  <LinkCard title="CLI Reference" href="/cli/" description="Install and manage skills." />
  <LinkCard title="agentskills.io" href="https://agentskills.io" description="The open ecosystem for cross-agent skills." />
</CardGrid>

---
[View source on GitHub](https://github.com/wyattowalsh/agents/blob/main/skills/skill-eval-scaffolder/SKILL.md)
