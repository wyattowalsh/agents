---
name: "redis-core"
description: "Install `redis-core` from the official Redis agent-skills bundle. The older `redis-development` slug is no longer exposed by the current source list."
title: "Redis Core"
source_kind: "curated-external"
source: "redis/agent-skills"
install_source: "redis/agent-skills"
status: "install-now-after-trust-gate"
trust_tier: "curated-trust-gated"
provenance_status: "verified-install-command"
sync_kind: "skills-cli"
install_command: "npx skills add redis/agent-skills --skill redis-core -y -g -a claude-code codex crush cursor opencode"
target_agents: ["claude-code", "codex", "crush", "cursor", "grok", "opencode"]
source_url: "https://github.com/redis/agent-skills"
notes: "Install `redis-core` from the official Redis agent-skills bundle. The older `redis-development` slug is no longer exposed by the current source list."
risk_notes: "Install `redis-core` from the official Redis agent-skills bundle. The older `redis-development` slug is no longer exposed by the current source list."
promotion_policy: "Install only after trust gate; audit again before repo promotion."
provenance_evidence: "Curated `npx skills add` command with named `--skill` selectors under `install-now-after-trust-gate` in authoring catalog."
---


Install `redis-core` from the official Redis agent-skills bundle. The older `redis-development` slug is no longer exposed by the current source list.
