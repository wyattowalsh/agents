---
name: "google-maps-scraper"
description: "Free open-source Google Maps scraper via Docker. Use when finding businesses, extracting leads, emails, reviews, or ratings from Google Maps (e.g. find all dentists in Berlin)."
title: "Google Maps Scraper"
source_kind: "curated-external"
source: "gosom/google-maps-scraper"
install_source: "gosom/google-maps-scraper@0ef302ecc72a8872d5dac68cbbeab78800f80fdd"
install_command: "npx skills add gosom/google-maps-scraper@0ef302ecc72a8872d5dac68cbbeab78800f80fdd --skill google-maps-scraper -y -g -a antigravity claude-code codex crush cursor gemini-cli github-copilot grok opencode"
install_skill_name: "google-maps-scraper"
status: "install-now-after-trust-gate"
trust_tier: "curated-trust-gated"
provenance_status: "verified-install-command"
sync_kind: "skills-cli"
target_agents: ["antigravity", "claude-code", "codex", "crush", "cursor", "gemini-cli", "github-copilot", "grok", "opencode"]
source_url: "https://github.com/gosom/google-maps-scraper"
selector_mode: "named"
audit_date: "2026-07-17"
audited_head: "0ef302ecc72a8872d5dac68cbbeab78800f80fdd"
license: "MIT"
pin_policy: "pin-before-install"
no_pin_rationale: ""
source_list_evidence: "source-list-found"
executable_surface: "Prompt/instruction skill installed through the Skills CLI. Runtime requires Docker and the gosom/google-maps-scraper image; no repo-vendored third-party code, hooks, MCP server, or background service is enabled by this catalog row."
credential_behavior: "No credentials required by default. Optional LeadsDB API keys and proxy credentials are user-owned and must stay out of the repo."
network_access: "When invoked, the skill runs a Docker container that scrapes Google Maps (and optionally business websites for emails). No background network service is enabled by the catalog row alone."
file_access: "Writes query/result files under /tmp (e.g. /tmp/gmaps_queries.txt, /tmp/gmaps_*.csv|json), uses Docker named volume gmaps-playwright-cache, and may copy results to user-chosen paths when asked."
live_action_risk: "Long-running Docker scrape jobs, Google platform/ToS and rate-limit risk, optional website crawling for emails, and high CPU/RAM browser automation. Invoke only on explicit user scrape requests; review flags before large grid searches."
risk_category: "standard-review"
dedupe_notes: "No name collision with repo-owned skills. Related but distinct catalog rows: seo-maps, google-flights, seatmaps, yc-jobs-scraper, apify/firecrawl scrapers."
notes: "Free open-source Google Maps scraper via Docker for business leads, emails, reviews, and ratings. Grok is a targetAgent via Claude/agents mirror; published install commands may omit the grok CLI token (no native Skills CLI adapter)."
risk_notes: "Google Maps scraping may conflict with Google ToS and can trigger blocking; keep user-directed and opt-in. Upstream skill Phase 1 text tells agents not to ask for permission and to proceed immediately into docker pull/run—this catalog row does NOT rewrite the installed SKILL.md. Harness global approval and destructive-action rules outrank that upstream auto-proceed text: require an explicit user scrape request with location and business type before any docker pull/run. Upstream CLI may send anonymous telemetry (DISABLE_TELEMETRY=1 to opt out). Affiliate/proxy links in the skill body are marketing, not install requirements. Skill metadata version may lag container image releases; re-audit HEAD before promotion bumps."
promotion_policy: "Installable curated row is allowed after source-list evidence, MIT license, single SKILL.md executable surface review, collision/dedupe review, local harness installation, and docs-steward catalog generation; re-run source/security review before adapting upstream content beyond this selected skill entry."
provenance_evidence: "Source-list evidence found selector google-maps-scraper in gosom/google-maps-scraper (npx skills add gosom/google-maps-scraper --list). Upstream license MIT and audited HEAD 0ef302ecc72a8872d5dac68cbbeab78800f80fdd captured 2026-07-17. Skills CLI accepts pinned source gosom/google-maps-scraper@0ef302ecc72a8872d5dac68cbbeab78800f80fdd (--list, 2026-07-17). Skill tree contains only skills/google-maps-scraper/SKILL.md (no scripts/hooks/MCP). Not present on security-quarantine hard-block slug list."
---

# Google Maps Scraper

This row integrates `gosom/google-maps-scraper` as an installable curated external skill after source, license, security, attribution, and harness review.

## Reviewed Integration

- Normalized URL: [https://github.com/gosom/google-maps-scraper](https://github.com/gosom/google-maps-scraper)
- Catalog skill id: `google-maps-scraper`
- Upstream install selector: `google-maps-scraper`
- Install command: `npx skills add gosom/google-maps-scraper@0ef302ecc72a8872d5dac68cbbeab78800f80fdd --skill google-maps-scraper -y -g -a antigravity claude-code codex crush cursor gemini-cli github-copilot grok opencode`
- Inspected commit SHA: `0ef302ecc72a8872d5dac68cbbeab78800f80fdd`
- Pin: install source pinned to audited HEAD (Skills CLI `@` ref verified via `--list`)
- License status: `MIT`
- Source-list evidence: `source-list-found`
- Executable surface: single `SKILL.md` (Docker-driven); no hooks/scripts packaged in the skill directory
- Live local install recorded: `true`

## Operator confirmation norm

Upstream Phase 1 instructs agents not to ask for permission and to proceed straight into Docker scrape jobs. **This catalog integration does not patch or vendor the installed `SKILL.md`.** Under this repository's global instructions:

1. Global approval, destructive-action, and live-network norms **outrank** upstream skill body text.
2. Agents must require an **explicit user scrape request** that includes location and business type (or equivalent unambiguous query) before `docker pull` / `docker run`.
3. Prefer shallow depth first; warn before grid search or long-running jobs.
4. Do not start scrapes from ambient context, background goals, or inferred intent alone.

Residual risk: skill-loaded agents still see upstream auto-proceed language. Mitigations are this norm, harness globals, and optional future upstream PR—not a local fork under `skills/`.

## Local Install Evidence

- Executed command (list): `npx skills add gosom/google-maps-scraper --list`
- List result: 1 skill `google-maps-scraper`
- Pin probe (2026-07-17): `npx skills add gosom/google-maps-scraper@0ef302ecc72a8872d5dac68cbbeab78800f80fdd --list` → Found 1 skill `google-maps-scraper`
- Executed command: `uv run wagents skills sync --apply` (catalog-driven multi-harness; exit 0, apply_failures 0)
- Executed command: `npx skills add gosom/google-maps-scraper --skill google-maps-scraper -y -g -a cursor -a codex -a opencode -a gemini-cli -a github-copilot -a antigravity -a claude-code -a crush`
- Skills CLI layout (2026-07-17): universal install under `~/.agents/skills/google-maps-scraper`; Claude Code and Crush symlink to that path; Cursor/Codex/OpenCode/Gemini CLI/GitHub Copilot discover via universal agents root (primary per-harness skill dirs may be MISS without failing discovery)
- Installed path: `~/.agents/skills/google-maps-scraper`
- Installed path: `~/.claude/skills/google-maps-scraper` (symlink → agents)
- Installed path: `~/.config/crush/skills/google-maps-scraper` (symlink → agents)
- Installed path: `~/.grok/skills/google-maps-scraper` (symlink → Claude/agents mirror)
- Live local install recorded: `true`

## Safety Notes

1. Require an explicit user scrape request (location + business type or clear query) before any scrape.
2. Require Docker installed and running before scrape jobs.
3. Do not auto-scrape from ambient context or skill auto-proceed language alone.
4. Prefer shallow depth first; warn before grid search (long runtime).
5. Never commit API keys, proxy passwords, or scrape outputs that contain personal data without user intent.
6. Optional telemetry opt-out for the scraper CLI: `DISABLE_TELEMETRY=1`.
7. Treat global approval / destructive-action rules as higher precedence than upstream Phase 1 text.

## Docs-Steward Notes

- This row is the installable catalog record for the matching reviewed external source.
- Do not vendor the upstream skill tree into `skills/`.
- Public generated install commands may strip the `grok` agent token (no native Skills CLI adapter); Grok coverage is via Claude/agents mirror after install.
- Regenerate with `uv run wagents docs generate --no-installed` after authoring changes.
- When committing, prefer path-scoped gms authoring + external page only if the skills-catalog-index diff still contains unrelated multi-skill names.
