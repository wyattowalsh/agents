---
name: "remotion-to-hyperframes"
description: "Port an existing Remotion (React) composition to HyperFrames HTML. Use ONLY when the user explicitly asks to port/convert/migrate/translate a Remotion source. Do NOT use: (a) authoring a new HyperFrames composition; (b) Remotion mentioned in passing; (c) Remotion code shared as reference only; (d) \"same video as my Remotion one\" without explicit migrate request — treat as fresh build. Doubt → `/general-video`. One-way, Remotion-only: no reverse export (HyperFrames→Remotion or any framework), no non-Remotion source (After Effects, Framer Motion, plain React/CSS) → out of scope, re-create via `/general-video`. Flags unsupported patterns (useState, useEffect, async calculateMetadata, third-party React libs, `@remotion/lambda`) and recommends runtime interop over lossy translation. Unsure whether to port vs. build fresh, or only a passing Remotion mention? → /hyperframes."
title: "Remotion To Hyperframes"
source_kind: "curated-external"
source: "heygen-com/hyperframes"
install_source: "heygen-com/hyperframes"
install_command: "npx skills add heygen-com/hyperframes --skill remotion-to-hyperframes -y -g -a antigravity claude-code codex crush cursor gemini-cli github-copilot grok opencode"
install_skill_name: "remotion-to-hyperframes"
status: "install-now-after-trust-gate"
trust_tier: "curated-trust-gated"
provenance_status: "verified-install-command"
sync_kind: "skills-cli"
target_agents: ["antigravity", "claude-code", "codex", "crush", "cursor", "gemini-cli", "github-copilot", "grok", "opencode"]
source_url: "https://github.com/heygen-com/hyperframes"
selector_mode: "named"
audit_date: "2026-07-07"
audited_head: "4c8064d4d3d1ffe51a10b1c7eee79a49b2fc468f"
license: "Apache-2.0"
pin_policy: "pin-before-install"
no_pin_rationale: "The Skills CLI source selector does not pin by commit in this harness command; the audited upstream HEAD is recorded for drift review before future promotion updates."
source_list_evidence: "source-list-found"
executable_surface: "Prompt/instruction skill installed through the Skills CLI; no repo-vendored third-party code, hooks, MCP server, daemon, or background service is enabled by this catalog row."
credential_behavior: "No credentials are required by the installed skill body by default; any downstream CLI, cloud, or account access remains user-owned and must stay out of the repo."
network_access: "No background network service is enabled by this catalog row; any network access is explicit, user-directed, and inherited from the invoked skill instructions or external CLI."
file_access: "Reads and writes only user-selected project files, generated artifacts, reports, or local tool outputs when explicitly invoked; do not process private data without user intent."
live_action_risk: "Installed skill runs with agent permissions when invoked; review commands before live cloud, account, filesystem, outreach, media, or destructive operations."
risk_category: "standard-review"
dedupe_notes: "Raw indexes covered: 156"
notes: "Port an existing Remotion (React) composition to HyperFrames HTML. Use ONLY when the user explicitly asks to port/convert/migrate/translate a Remotion source. Do NOT use: (a) authoring a new HyperFrames composition; (b) Remotion mentioned in passing; (c) Remotion code shared as reference only; (d) \"same video as my Remotion one\" without explicit migrate request — treat as fresh build. Doubt → `/general-video`. One-way, Remotion-only: no reverse export (HyperFrames→Remotion or any framework), no non-Remotion source (After Effects, Framer Motion, plain React/CSS) → out of scope, re-create via `/general-video`. Flags unsupported patterns (useState, useEffect, async calculateMetadata, third-party React libs, `@remotion/lambda`) and recommends runtime interop over lossy translation. Unsure whether to port vs. build fresh, or only a passing Remotion mention? → /hyperframes."
risk_notes: "Installed skill runs with agent permissions when invoked; review commands before live cloud, account, filesystem, outreach, media, or destructive operations."
promotion_policy: "Installable curated row is allowed after source-list evidence, compatible license metadata, collision/dedupe review, local harness installation, and docs-steward catalog generation; re-run source/security review before adapting upstream content beyond this selected skill entry."
provenance_evidence: "Source-list evidence found selector `remotion-to-hyperframes` in `heygen-com/hyperframes`. Upstream license `Apache-2.0` and audited HEAD `4c8064d4d3d1ffe51a10b1c7eee79a49b2fc468f` were captured before promotion. Local Skills CLI installed the containing collection and repaired duplicate owner state for selected collisions. This row records dedupe/collision handling and keeps external services opt-in."
---

# Remotion To Hyperframes

This row promotes `heygen-com/hyperframes` from the July 2026 candidate corpus into an installable curated external skill.

## Reviewed Integration

- Raw indexes covered: 156
- Normalized URL: [https://github.com/heygen-com/hyperframes](https://github.com/heygen-com/hyperframes)
- Catalog skill id: `remotion-to-hyperframes`
- Upstream install selector: `remotion-to-hyperframes`
- Install command: `npx skills add heygen-com/hyperframes --skill remotion-to-hyperframes -y -g -a antigravity claude-code codex crush cursor gemini-cli github-copilot grok opencode`
- Inspected commit SHA: `4c8064d4d3d1ffe51a10b1c7eee79a49b2fc468f`
- License status: `Apache-2.0`
- Source-list evidence: `source-list-found`
- Live local install recorded: `true`

## Local Install Evidence

- Executed command: `npx --yes skills add https://github.com/heygen-com/hyperframes --list`
- Executed command: `npx skills add https://github.com/heygen-com/hyperframes -y -g -a antigravity claude-code codex crush cursor gemini-cli github-copilot opencode`
- Executed command: `npx skills add https://github.com/heygen-com/hyperframes --skill remotion-to-hyperframes -y -g -a antigravity claude-code codex crush cursor gemini-cli github-copilot opencode`
- Installed path: `~/.agents/skills/remotion-to-hyperframes`
- Installed path: `~/.claude/skills/remotion-to-hyperframes`
- Installed path: `~/.config/crush/skills/remotion-to-hyperframes`
- Install evidence note: Live collection install plus collision repair installed this skill into supported Skills CLI harness roots. The Skills CLI has no native grok adapter in this environment; Grok follows this repo convention by consuming Claude-compatible mirrored skills. Verified install roots: ~/.agents/skills, ~/.claude/skills, and ~/.config/crush/skills.

## Safety Notes

- Credential behavior: No credentials are required by the installed skill body by default; any downstream CLI, cloud, or account access remains user-owned and must stay out of the repo.
- Network access: No background network service is enabled by this catalog row; any network access is explicit, user-directed, and inherited from the invoked skill instructions or external CLI.
- File access: Reads and writes only user-selected project files, generated artifacts, reports, or local tool outputs when explicitly invoked; do not process private data without user intent.
- Live action risk: Installed skill runs with agent permissions when invoked; review commands before live cloud, account, filesystem, outreach, media, or destructive operations.
- MCP, background services, and external binaries remain opt-in; do not auto-start them from this catalog row.
- Secrets, tokens, private keys, connection strings, cookies, OAuth grants, and account IDs must not be committed.

## Docs-Steward Notes

- This row is the installable catalog promotion for the matching candidate-corpus intake row.
- Auth, install, validation, attribution, and promotion evidence remain visible in the candidate corpus manifests.
