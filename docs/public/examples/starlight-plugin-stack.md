# Starlight Plugin Stack Proof

This file is served from the docs public directory so the SaveFile component has a stable download target.

Enabled surface groups:

- Markdown: GitHub alerts, directive asides, markdown blocks, Mermaid, live code.
- Reading UX: image zoom, keyboard shortcut rendering, fullscreen code blocks, scroll to top.
- Registries: link validation, llms.txt generation, site graph.
