# Tasks

- [x] Add `wagents/context.py` repo discovery and CLI context
- [x] Add `wagents self` subcommands and doctor extensions
- [x] Add plugin loader and opt-in telemetry
- [x] Update platform path resolution for lazy repo root
- [x] Add tests and CI wheel smoke
- [x] Regenerate CLI docs and bundle metadata