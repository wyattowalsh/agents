# Affected Surfaces

Owned in this pass: `planning/20-harness-registry/github-copilot-*.md` and `openspec/changes/agents-c04-copilot-harness/`.

Future implementation inputs: `platforms/copilot/`, `.github/instructions/`, `.github/prompts/`, and `.github/skills/`. `.github/copilot-instructions.md` is excluded unless docs consolidation schedules it.
