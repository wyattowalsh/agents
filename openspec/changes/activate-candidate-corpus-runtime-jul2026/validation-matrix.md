# Validation matrix: activate-candidate-corpus-runtime-jul2026

| Gate | Evidence | Expected |
| --- | --- | --- |
| Corpus | 293 raw, 289 normalized, four duplicate groups | exact |
| Artifact discovery | exact entrypoint-backed artifact graph | no unclassified executable surfaces |
| Selectors | recompute from the current authoritative promotion source plus exact path/hash receipts | current snapshot derives exactly 1,266 selectors and 7,596 declared harness bindings; copied historical constants fail |
| Packages | install, use, failure, denial, rollback, reinstall | every artifact accepted |
| Skills / RV-001 | distinct content-addressed receipts for every selector-to-harness binding across discovery, behavior, fresh-process, rollback, and promoted-final phases | `already_present` remains discovery-only; exact selector, harness, current input digest, and installed digest match |
| Capabilities / RV-001 | derive required capabilities from current portable catalog/sync metadata and proved capabilities from accepted behavioral leaves | `untested = required - proved`; no hardcoded empty untested set |
| Behavioral receipt precondition | ordinary CLI/plugin process-group cleanup regression gate from `remediate-rv-skill-docs-contracts` | receipt regeneration remains blocked until TERM → bounded wait → KILL → reap/drain tests pass |
| MCP | initialize, tools/list, harmless call, restart, fresh discovery | enabled only after canary |
| MCP / RV-002 source-closed | `uv run pytest -q tests/test_record_candidate_mcp_activation.py tests/test_candidate_runtime_activation.py -k 'mcp or reachability or auth'` | explicit registry `enabled: true`; registry/generated/live presence and identity equal; authenticated reachability and unauthenticated denial remain green |
| Plugin provenance | `verify_candidate_plugin_provenance.py`; pinned commit/tree/projection lock; marketplace and live digest checks | all eight enabled plugins reproduce the approved digest from Git objects; symlinks, submodules, generated bytecode, and content drift fail closed |
| Plugins | `run_candidate_plugin_canaries.py`; `rehearse_candidate_plugin_rollback.py`; exact version/content digests | eight enabled plugins pass; nine disabled plugins remain fail-closed |
| Auth | minimum scope, positive/negative use, revoke/logout | no stored secret values |
| Quarantine | lawful isolated use, global rejection, cleanup | no global discovery |
| Transactions | shared key-CAS lock, declared writes, crash safety, exact preimage rollback | disjoint commits survive; conflicts and failures preserve prior bytes; no duplicate or mismatched artifact set can be committed |
| Evidence | immutable transaction-scoped journal/transcript schemas and digests; `commit-pending` journal plus post-CAS passed marker | marker binds the exact artifact set and receipt-store transaction; failure evidence and markerless commits remain incomplete; no-follow descriptor reads reject symlink escape |
| Review / RV-004 | `uv run pytest -q tests/test_record_candidate_final_closure.py -k review` plus trusted-issuer integration evidence when available | self-authored issuer identities rejected; externally issued session/task provenance accepted; otherwise live review closure is `BLOCKED-EXTERNAL` |
| Provenance claims | review source and public closure text | no DSSE, SLSA, in-toto, or PKI compliance claim without a separately adopted and proved implementation |
| Migration | v1 check/apply with frozen preimage and per-key status | only verified proof migrates; missing proof remains incomplete |
| Docs | source/generator/output/validator edges | synchronized and idempotent |
| Closure | exact selector/binding receipts; `record_candidate_runtime_activation.py --check`; strict `--require-complete` negative gate | selector and binding gates accepted; zero runtime blockers required before full-usability claim |
| OpenSpec | `uv run wagents openspec status --change activate-candidate-corpus-runtime-jul2026 --format json`; `npx -y @fission-ai/openspec@latest validate activate-candidate-corpus-runtime-jul2026 --type change --strict --json --no-interactive`; `uv run wagents openspec validate --strict --format json` | successor change is inspectable and targeted plus full-project strict validation pass |
