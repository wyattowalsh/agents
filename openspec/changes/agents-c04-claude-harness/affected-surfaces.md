# Affected Surfaces

Owned in this pass: `planning/20-harness-registry/claude-*.md` and `openspec/changes/agents-c04-claude-harness/`.

Future implementation inputs: `.claude-plugin/` and Claude fixtures. Shared root docs and `CLAUDE.md` are out of scope for this pass.
