# Affected Surfaces

Owned in this pass: `planning/20-harness-registry/opencode.md`, `planning/20-harness-registry/gemini-cli.md`, `planning/20-harness-registry/antigravity.md`, and this child OpenSpec.

Future implementation inputs: `.opencode/`, `.opencode-plugin/`, `.gemini/`, `.antigravity/`, and `opencode.json`. `instructions/opencode-global.md` is excluded unless docs coordination schedules it.
