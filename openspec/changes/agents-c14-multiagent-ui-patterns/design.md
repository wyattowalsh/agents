# Design

Capture multiagent UI patterns as requirements input. Task boards, run graphs, review queues, support matrices, and replay timelines are described as abstractions with adoption risks and redaction requirements.
