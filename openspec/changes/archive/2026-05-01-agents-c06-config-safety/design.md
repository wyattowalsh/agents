# Design

Define config safety as a transaction model with preview, apply, rollback, backup snapshots, redaction, environment overlays, sandbox profiles, and policy exceptions. This pass records the contract without applying config changes.
