## ADDED Requirements

### Requirement: Context graph capability evaluation

The knowledge graph/context lane SHALL evaluate graph and memory capabilities through a skills-first model with optional MCP only for justified live queries.

#### Scenario: Graph capability is deterministic

- **GIVEN** a graph capability can be generated from local repository files
- **WHEN** integration is evaluated
- **THEN** the recommended default is an Agent Skill or CLI workflow.
