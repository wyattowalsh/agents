# Affected Surfaces

Owned in this pass: `planning/20-harness-registry/cursor-*.md` and `openspec/changes/agents-c04-cursor-harness/`.

Future implementation inputs: `.cursor/` and Cursor fixtures. Root docs and generated support matrices are out of scope.
