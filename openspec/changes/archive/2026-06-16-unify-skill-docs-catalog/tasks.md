# Tasks

## Completed

- [x] Unified `collect_skill_doc_nodes` and catalog MDX generation
- [x] Hub pages use LinkCard indexes (no `SkillCatalog`)
- [x] Legacy `/skills/<name>/` → `/skills/catalog/<name>/` redirects
- [x] Sync desired-set: repo-owned + Install Now curated merge
- [x] `wagents docs research` batch planner + manifest
- [x] Retire `SkillCatalog.astro` / `SkillCatalog.tsx`

## Follow-up

- [x] Phase A research artifacts for custom skills (`docs/src/skill-research/`)
- [x] Optional CI `--check-research` gate on release branches
- [x] Promote change to `openspec/specs/docs-instructions/spec.md`
