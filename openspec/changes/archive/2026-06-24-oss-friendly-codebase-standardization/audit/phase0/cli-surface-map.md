# CLI Surface Map

See audit phase0 bootstrap. Key gaps: install/update/readme/grok doctor lack --format json.

Per-harness sync: scripts/sync_agent_stack.py + wagents/platforms/*.py (not top-level wagents sync).
