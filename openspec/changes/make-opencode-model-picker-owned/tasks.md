# Tasks

## Implementation

- [x] Update OpenCode config generators so build and plan defaults are repo-managed.
- [x] Update repo `opencode.json` with explicit root, build, plan, and provider variant defaults.
- [x] Update global `~/.config/opencode/opencode.json` with explicit root, build, plan, and provider variant defaults.
- [x] Update TUI model picker state for max/high variant selection across requested providers.

## Documentation

- [x] Update repo OpenCode instructions for repo-managed build and plan defaults.
- [x] Update OpenCode-specific instructions for repo-managed build and plan defaults.

## Verification

- [x] Validate JSON surfaces.
- [x] Run targeted OpenCode config tests.
- [x] Run OpenCode sync drift checks.
- [x] Validate this OpenSpec change.
