# Affected Surfaces

Owned in this pass: `planning/50-config-safety/00-config-transaction-blueprint.md` and this child OpenSpec.

Future implementation inputs: `config/`, `hooks/`, transaction fixtures, and tests. Live user config outside the repo is out of scope and no-touch unless explicitly approved.
