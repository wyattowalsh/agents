# Design

Define quality gates before workflow/test implementation. Registry schemas, skill/MCP conformance, eval scenarios, deterministic replay, and report artifacts are specified as contracts first to avoid colliding with unrelated dirty tests.
