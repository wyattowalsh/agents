# Design

Define telemetry as a redacted local artifact model first. Session replay, run graph, token/cost fields, retention, and deletion rules are specified before any collection or export implementation.
