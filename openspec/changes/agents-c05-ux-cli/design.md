# Design

Define CLI and dashboard output contracts before changing implementation. Human output, JSON shape, dashboard grouping, and redaction requirements are documented first, with golden snapshot tests deferred until implementation is scheduled.
